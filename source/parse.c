#include <string.h>
#include <stdlib.h>
#include <stdio.h>

#include "parse.h"
#include "lexer.h"

int astgen(token_t **tokens, int *num_tokens, void *AST) {
    obj_t *objects = malloc(sizeof(obj_t) * 1024);
    
    

    free(objects);
    return 0;
}